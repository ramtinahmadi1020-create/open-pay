{
  "name": "open-pay",
  "version": "1.0.0",
  "description": "A secure, serverless payment gateway on Cloudflare Workers.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14"
  }
}
